tls on
port 587
tls_trust_file /etc/ssl/certs/ca-certificates.crt
tls_starttls on
account <your account at your provider for mail-relay>
host <the host for sending mails, for example for T-Online: securesmtp.t-online.de>
from <the mailadress which is used as sender-mail-address>
auth login
user <the user-id to login the smtp-server: in very most cases the mailaddress>
password <the password for login into smtp-server>
logfile <name of a logfile for msmtp, for example /home/pi/.msmtp.log (logilfe must start with a '.')>
domain <domain-name of your computer, for example raspberry>
